import Header from '../Header'
import SideBar from '../SideBar'

const SavedVideos = () => (
  <div>
    <Header />
    <div>
      <SideBar />
      <p>hello save</p>
    </div>
  </div>
)
export default SavedVideos
